import { DynamODMError } from './dynamodm.error';
import { NoOutputError } from './no-output.error';

/**
 * One object to hold all error classes.
 */
const Errors = {
  DynamODMError,
  NoOutputError,
};

export {
  Errors,
  DynamODMError,
  NoOutputError,
};
